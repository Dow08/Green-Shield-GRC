fastapi==0.115.5
uvicorn[standard]==0.32.1
PyYAML==6.0.2

# Requis par la route d'import de configuration client (UploadFile/File dans
# api/modules/projects.py). FastAPI lève une RuntimeError **au moment de
# l'import du module** si le paquet est absent : sans lui l'API ne démarre pas
# du tout. L'oubli est resté invisible jusqu'au premier run de CI (29/07/2026),
# le paquet étant présent par ailleurs sur le poste de développement.
python-multipart==0.0.32

# Archives de mission chiffrées en AES-256 (WinZip AES, lisible par 7-Zip et
# WinZip). `zipfile` de la stdlib sait lire un zip chiffré mais pas en écrire.
# Wheels précompilés pour Windows et Linux : aucune compilation requise
# (cf. CLAUDE.md, règle 1 — aucune dépendance native obligatoire en local).
pyzipper==0.4.0

# Génération du rapport Word (api/modules/report_docx.py) : le document est
# construit directement en python-docx, sans gabarit intermédiaire.
# Pur Python, aucune dépendance native -> s'installe à l'identique sous Windows
# et Linux (cf. docs/audit-critique-plan.md, F6 : règle "aucune dépendance
# native obligatoire pour le développement local").
python-docx==1.2.0

pytest==8.3.3
cryptography>=41.0.0
slowapi>=0.1.9
markdown==3.7

sqlalchemy==2.0.51

passlib[bcrypt]==1.7.4
PyJWT[crypto]>=2.13.0
